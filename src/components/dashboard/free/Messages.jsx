// Free user Messages – uses shared Messages; free users see upgrade CTA (canMessage false).
import Messages from "../shared/Messages.jsx";

export default function FreeMessages() {
  return <Messages />;
}
