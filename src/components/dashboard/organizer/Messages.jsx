// Organiser Messages – uses shared Messages (bandhan-chat-service); organisers have canMessage.
import Messages from "../shared/Messages.jsx";

export default function OrganizerMessages() {
  return <Messages />;
}
